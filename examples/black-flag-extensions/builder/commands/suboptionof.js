// @ts-check

/**
 * TODO: Add examples of "implying something only when a boolean flag is given
 * TODO: via CLI and also set to false" which cannot be accomplished with normal
 * TODO: implies, it needs subOptionOf (potentially on itself). A real life
 * TODO: example is in symbiote's lint/test command builders
 */

export {};
